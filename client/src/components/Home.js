import React from 'react';
import '../App.css';

const Home = () => {
    return (
        <div>
            <p>
                This is the homepage of a simple example of using React to Query the MARVEL API.
			</p>
            <p>
                You can start by clicking buttons above.
			</p>
        </div>
    );
};

export default Home;
